@page "/login"
@using YbsSmartCardSystem.Contracts.Features.Auth
@inject YbsSmartCardSystem.App.Services.ApiService Api
@inject YbsSmartCardSystem.App.Services.AuthStateService AuthState
@inject NavigationManager NavManager

<div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-slate-950 via-slate-900 to-indigo-950 p-4">
    <div class="w-full max-w-md bg-slate-900/60 backdrop-blur-xl border border-white/10 rounded-3xl p-8 shadow-2xl transition-all duration-300 hover:shadow-indigo-500/10">
        
        <div class="text-center mb-8">
            <div class="inline-flex items-center justify-center w-16 h-16 rounded-2xl bg-gradient-to-tr from-indigo-500 to-violet-500 mb-4 shadow-lg shadow-indigo-500/30">
                <span class="bi bi-shield-lock-fill text-white text-2xl"></span>
            </div>
            <h2 class="text-2xl font-bold text-white tracking-tight">Welcome Back</h2>
            <p class="text-sm text-slate-400 mt-1">YBS Smart Card System Management Portal</p>
        </div>

        @if (!string.IsNullOrEmpty(ErrorMessage))
        {
            <div class="mb-6 p-4 rounded-xl bg-rose-500/10 border border-rose-500/20 flex items-start gap-3">
                <span class="bi bi-exclamation-triangle-fill text-rose-400 mt-0.5"></span>
                <span class="text-sm text-rose-300 font-medium">@ErrorMessage</span>
            </div>
        }

        <EditForm Model="@Model" OnValidSubmit="HandleSubmit" FormName="LoginForm">
            <AntiforgeryToken />
            <DataAnnotationsValidator />
            <div class="space-y-5">
                <div>
                    <label class="block text-xs font-semibold text-slate-400 uppercase tracking-wider mb-2">Phone Number</label>
                    <div class="relative">
                        <span class="absolute inset-y-0 left-0 pl-3.5 flex items-center text-slate-500">
                            <span class="bi bi-telephone-fill"></span>
                        </span>
                        <InputText class="w-full bg-slate-950/40 border border-white/10 text-white rounded-xl pl-10 pr-4 py-3 focus:outline-none focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 transition-all duration-200 text-sm" 
                                   placeholder="Enter your phone number" @bind-Value="Model.PhoneNumber" />
                    </div>
                </div>

                <div>
                    <label class="block text-xs font-semibold text-slate-400 uppercase tracking-wider mb-2">Password</label>
                    <div class="relative">
                        <span class="absolute inset-y-0 left-0 pl-3.5 flex items-center text-slate-500">
                            <span class="bi bi-lock-fill"></span>
                        </span>
                        <InputText type="password" class="w-full bg-slate-950/40 border border-white/10 text-white rounded-xl pl-10 pr-4 py-3 focus:outline-none focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 transition-all duration-200 text-sm" 
                                   placeholder="••••••••" @bind-Value="Model.Password" />
                    </div>
                </div>

                <div class="pt-2">
                    <button type="submit" class="w-full py-3.5 bg-gradient-to-r from-indigo-500 to-violet-600 hover:from-indigo-600 hover:to-violet-700 text-white font-semibold rounded-xl shadow-lg shadow-indigo-500/20 active:scale-[0.98] transition-all duration-200 disabled:opacity-50 disabled:pointer-events-none" 
                            disabled="@IsSubmitting">
                        @if (IsSubmitting)
                        {
                            <span class="inline-block animate-spin border-2 border-white border-t-transparent rounded-full w-4 h-4 mr-2 vertical-middle"></span>
                            <span>Logging in...</span>
                        }
                        else
                        {
                            <span>Sign In</span>
                        }
                    </button>
                </div>
            </div>
        </EditForm>

        <div class="mt-6 text-center text-sm text-slate-400">
            Don't have an account? 
            <a href="/register" class="text-indigo-400 font-semibold hover:text-indigo-300 transition-colors">Sign up here</a>
        </div>
    </div>
</div>
